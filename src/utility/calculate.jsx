const add = (first, second) => {
  return first + second;
};
const multiply = (first, second) => {
  return first * second;
};
const devideTwoNumbersAndReturnResult = (first, second) => {
  return first / second;
};
// export default multiply;
export { add, multiply, devideTwoNumbersAndReturnResult };
